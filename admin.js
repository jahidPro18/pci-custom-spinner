
            jQuery(document).ready(
                function ($) {
                    // Color picker initialization
                    if ($.fn.wpColorPicker) {
                        $('.color-picker').wpColorPicker();
                    }
                }
            );
            